import styled from "styled-components";

export const Wrapper = styled.div`
    display: flex;
    align-items: center;
    gap: 10px;

    
   button {
    background-color: var(--darkPink);
    padding: 5px 10px;
    border-radius: 10px;
    margin: 20px;
    cursor: pointer;
   }
`;